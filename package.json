{
  "name": "opoweb",
  "version": "0.1.0",
  "description": "Plataforma local de tests de oposiciones con importacion desde PDFs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "import:pdfs": "python scripts/import_pdfs.py"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
